spring:
  datasource:
    url: jdbc:postgresql://localhost/udemydb
    username: postgres
    password: superhardpass12
  jpa:
    show-sql: true
    database: postgresql
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect